/**
 * Nombre de la sección y del ítem que lista, en un único lugar.
 *
 * El usuario avisó explícitamente (2026-09-02) que "Mis gestiones" es el
 * nombre que el negocio dio ahora, pero que es probable que lo vuelvan a
 * cambiar — de ahí que viva acá como constante en vez de estar hardcodeado
 * en cada pantalla/copy. Si el nombre cambia, se edita sólo acá.
 */

/** Nombre de la sección (título de pantalla, menús, links al Hub). */
export const SECTION_LABEL = 'Mis gestiones'

/** Nombre de un ítem individual del listado, singular y plural. */
export const ITEM_LABEL_SINGULAR = 'gestión'
export const ITEM_LABEL_PLURAL = 'gestiones'
